# Opt out of GitHub Actions log grouping; exercised by
# `config_opt_out_disables_grouping_under_github_actions` and
# `group_output_opt_out_drops_annotations_but_keeps_the_summary` in
# tests/chain_integration.rs.
[github]
group_output = false
