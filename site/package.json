{
  "name": "runner-site",
  "version": "0.0.0",
  "private": true,
  "description": "runner.kjanat.com — static site for the runner CLI",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.87.0"
  }
}
