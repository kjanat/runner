#:schema /home/kjanat/projects/gist/cargo-config.schema/cargo-config.schema.json

[alias]
l          = "lint"
lint       = "clippy --all-targets --all-features -- -D warnings -D clippy::all"
bin-run    = "run --bin run"
bin-runner = "run --bin runner"
t          = "test --all-features"
